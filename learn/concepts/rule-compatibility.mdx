---
title: "Rule Compatibility"
description: "How Lotus tests resolution equivalence."
---

**Rule compatibility** is the test Lotus applies to determine if two venue markets resolve the same way under the same conditions. Inputs include resolution source, timing, edge case handling, and settlement values. The output is `compatible`, `partially compatible`, or `incompatible`.