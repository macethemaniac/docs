---
title: "Understanding fallbacks"
description: "When Lotus can recover a route and when it must fail closed."
---

If a leg of a multi-venue route fails to fill, Lotus may attempt a pre-declared fallback route within the same EST. If no fallback is safe, Lotus fails closed and the execution is canceled.